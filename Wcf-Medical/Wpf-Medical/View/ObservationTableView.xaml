<Page x:Class="Wpf_Medical.View.ObservationTableView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="700"
	Title="ObservationTableView">

    <Grid>
        <StackPanel Orientation="Vertical">
            <Label Content="Observations" Height="35" HorizontalAlignment="Center" Name="labelTitle" VerticalAlignment="Top" FontSize="20" FontFamily="Arial Unicode MS" Width="138" Margin="20" />
            <DataGrid Height="267" HorizontalAlignment="Center" Margin="20" Name="gridPatients" VerticalAlignment="Top" Width="407">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="110" Binding="{Binding Name}" Header="Patient"></DataGridTextColumn>
                    <DataGridTextColumn Width="98" Binding="{Binding Data}" Header="Date"></DataGridTextColumn>
                    <DataGridTextColumn Width="90" Binding="{Binding Poids}" Header="Poids"></DataGridTextColumn>
                    <DataGridTextColumn Width="110" Binding="{Binding PressionSanguine}" Header="Pression Sanguine"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
